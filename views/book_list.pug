extends layout

block content
  h1 Список книг
  ul
    each book in book_list
      li
        a(href=book.url) #{book.title}
        |  (by #{book.author ? book.author.family_name + ', ' + book.author.first_name : 'Unknown'})
    else
      li No books available.
